<Window x:Class="IdeaMaker.Dialogs.ApiKeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API Key 设置" Height="280" Width="400"
        Background="#1e1e2e"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="CaretBrush" Value="#cdd6f4"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="API Key 设置" Foreground="#89b4fa" FontSize="14" FontWeight="Bold" Margin="0 0 0 12"/>
        <TextBlock Text="请输入 DeepSeek API Key" Foreground="#6c7086" FontSize="12" Margin="0 0 0 8"/>
        <PasswordBox x:Name="ApiKeyBox" FontFamily="Consolas" FontSize="12" Height="32" Padding="8" Margin="0 0 0 8"/>
        <TextBlock x:Name="StatusText" Foreground="#a6e3a1" FontSize="11" Margin="0 0 0 12" Visibility="Collapsed"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="ValidateBtn" Click="ValidateBtn_Click"
                    Content="验证" Height="30" Margin="0 0 6 0"/>
            <Button Grid.Column="1" x:Name="SaveBtn" Click="SaveBtn_Click"
                    Content="保存" Height="30" Background="#89b4fa" Foreground="#1e1e2e" FontWeight="SemiBold" BorderThickness="0"/>
        </Grid>

        <Button x:Name="CancelBtn" Click="CancelBtn_Click"
                Content="取消" Height="24" Foreground="#6c7086" Background="Transparent"
                BorderThickness="0" HorizontalAlignment="Center" Margin="0 12 0 0" Visibility="Collapsed"/>
    </StackPanel>
</Window>
