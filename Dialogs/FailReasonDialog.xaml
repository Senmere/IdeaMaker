<Window x:Class="IdeaMaker.Dialogs.FailReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="未完成原因" Height="320" Width="420"
        Background="#1e1e2e"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="CaretBrush" Value="#cdd6f4"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="任务未完成" Foreground="#89b4fa" FontSize="14" FontWeight="Bold" Margin="0 0 0 8"/>
        <TextBlock Text="请输入未完成的原因" Foreground="#6c7086" FontSize="12" Margin="0 0 0 8"/>
        <TextBox x:Name="ReasonBox" FontSize="13" Padding="10"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 TextChanged="ReasonBox_TextChanged" Margin="0 0 0 12"/>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
            <TextBlock Text="将扣除积分:" Foreground="#6c7086" FontSize="12" VerticalAlignment="Center"/>
            <Label x:Name="DeductText" Content="3" Foreground="#f38ba8" FontSize="16" FontWeight="Bold" Margin="8 0 0 0"/>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Cancel_Click" Content="取消" Height="30" Margin="0 0 6 0"/>
            <Button Grid.Column="1" Click="Confirm_Click" Content="确认" Height="30" Background="#f38ba8" Foreground="#1e1e2e" FontWeight="SemiBold" BorderThickness="0"/>
        </Grid>
    </StackPanel>
</Window>
