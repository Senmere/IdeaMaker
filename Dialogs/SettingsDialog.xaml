<Window x:Class="IdeaMaker.Dialogs.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="500" Width="500"
        Background="#1e1e2e"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="CaretBrush" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#181825" BorderBrush="#313244" BorderThickness="0 0 0 1">
            <DockPanel Margin="16 10">
                <TextBlock Text="设置" Foreground="#89b4fa" FontSize="14" FontWeight="Bold"/>
                <Button DockPanel.Dock="Right" Click="Close_Click" Content="关闭" Width="50" Height="24"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="20">
            <StackPanel>
                <TextBlock Text="API Key" Foreground="#bac2de" FontSize="13" FontWeight="SemiBold" Margin="0 0 0 8"/>
                <Grid Margin="0 0 0 20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <PasswordBox x:Name="ApiKeyBox" FontFamily="Consolas" FontSize="12" Height="32" Padding="8"/>
                    <Button Grid.Column="1" Click="EditApiKey_Click" Content="修改" Width="60" Height="32" Margin="8 0 0 0"/>
                </Grid>

                <TextBlock Text="系统提示词" Foreground="#bac2de" FontSize="13" FontWeight="SemiBold" Margin="0 0 0 8"/>
                <TextBox x:Name="PromptBox" FontSize="12" Padding="10" Height="160"
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                <TextBlock Text="留空使用默认提示词" Foreground="#6c7086" FontSize="11" Margin="0 8 0 0"/>

                <DockPanel Margin="0 16 0 0">
                    <TextBlock Text="默认提示词" Foreground="#89b4fa" FontSize="12" FontWeight="SemiBold" Margin="0 0 0 6" DockPanel.Dock="Top"/>
                    <Button DockPanel.Dock="Bottom" Click="CopyDefault_Click" Content="复制" Width="50" Height="24" HorizontalAlignment="Right" Margin="0 6 0 0"/>
                    <TextBox x:Name="DefaultPromptBox" FontSize="11" Padding="10" Height="80"
                             TextWrapping="Wrap" IsReadOnly="True" IsReadOnlyCaretVisible="False"
                             VerticalScrollBarVisibility="Auto" Foreground="#6c7086"/>
                </DockPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Reset_Click" Content="恢复默认" Height="30"/>
            <Button Grid.Column="1" Click="Save_Click" Content="保存" Height="30" Background="#89b4fa" Foreground="#1e1e2e" FontWeight="SemiBold" BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>
