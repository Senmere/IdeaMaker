<Window x:Class="IdeaMaker.Windows.TrashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="垃圾桶" Height="520" Width="420"
        Background="#1e1e2e"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#1e1e2e"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#181825" BorderBrush="#313244" BorderThickness="0 0 0 1">
            <DockPanel Margin="16 10">
                <TextBlock Text="垃圾桶" Foreground="#89b4fa" FontSize="14" FontWeight="Bold"/>
                <Button DockPanel.Dock="Right" Click="Close_Click" Content="关闭" Width="50" Height="24"/>
            </DockPanel>
        </Border>

        <ListBox x:Name="TrashList" Grid.Row="1" Margin="12" BorderThickness="1"
                 SelectionChanged="TrashList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#313244" BorderBrush="#45475a" BorderThickness="1" Padding="12" Margin="0 0 0 4">
                        <StackPanel>
                            <DockPanel>
                                <TextBlock Text="{Binding Topic}" Foreground="#cdd6f4" FontSize="13" FontWeight="Medium"/>
                                <TextBlock DockPanel.Dock="Right" Foreground="#f9e2af" FontSize="11" HorizontalAlignment="Right">
                                    <Run Text="难度 "/><Run Text="{Binding Difficulty, Mode=OneWay}"/>
                                </TextBlock>
                            </DockPanel>
                            <TextBlock Text="{Binding Description}" Foreground="#6c7086" FontSize="12" TextWrapping="Wrap" MaxHeight="44" Margin="0 4 0 0"/>
                            <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                                <TextBlock Foreground="#f38ba8" FontSize="11">
                                    <Run Text="原因: "/><Run Text="{Binding Reason, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Foreground="#f38ba8" FontSize="11" Margin="12 0 0 0">
                                    <Run Text="已扣"/><Run Text="{Binding PointsDeducted, Mode=OneWay}"/><Run Text="分"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="RestoreBtn" Click="Restore_Click" Content="稍后完成" Height="30" Background="#a6e3a1" Foreground="#1e1e2e" FontWeight="SemiBold" BorderThickness="0" IsEnabled="False"/>
            <Button Grid.Column="1" x:Name="ViewBtn" Click="View_Click" Content="查看详情" Height="30" Margin="8 0 0 0" IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>
